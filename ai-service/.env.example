# Umbral mínimo de similitud coseno (0.0 - 1.0) para considerar un match válido
SIMILARITY_THRESHOLD=0.55

# Modelo CLIP a usar (ver open_clip.list_pretrained() para otras opciones)
CLIP_MODEL_NAME=ViT-B-32
CLIP_PRETRAINED=openai
